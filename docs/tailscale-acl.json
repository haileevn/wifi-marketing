{
  "acls": [
    {
      "action": "accept",
      "src": ["tag:portal"],
      "dst": ["tag:router:22"]
    },
    {
      "action": "accept",
      "src": ["autogroup:member"],
      "dst": ["tag:router:22"]
    },
    {
      "action": "accept",
      "src": ["tag:portal"],
      "dst": ["tag:router:41641"]
    },
    {
      "action": "accept",
      "src":    ["autogroup:member"],
      "dst":    ["autogroup:internet:*"]
    }
  ],
  "tagOwners": {
    "tag:portal": ["autogroup:admin"],
    "tag:router": ["autogroup:admin"]
  },
  "ssh": [
    {
      "action": "accept",
      "src": ["tag:portal"],
      "dst": ["tag:router"],
      "users": ["root"]
    }
  ],
  "_comment": [
    "Dán vào Tailscale Admin → Access controls (điều chỉnh theo org).",
    "VPS portal: tailscale up --advertise-tags=tag:portal",
    "Router enroll: authkey gắn tag:router (Reusable, không ephemeral).",
    "Không mở SSH router ra Internet — chỉ mesh Portal → Router:22."
  ]
}
